import currentPage from './currentPage.js';
import {getJsonDiff, getSettings} from './settings.js';

export {
    currentPage,
    getJsonDiff,
    getSettings
}

export default {
    currentPage,
    getJsonDiff,
    getSettings
}